The .sly file can be analyzed with
a text editor. .npz package can be opened directly with numpy.

For more info, check out:
	http://docs.scipy.org/doc/numpy/reference/generated/numpy.savez.html#numpy.savez

To run script type in console (without the quotes):
	"python ply2npz.py plane"